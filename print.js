const { Router } = require('express');
const { } = require('multer');

const router = Router({
    caseSensitive: true,
    strict: true
});

router.post('/', (request, response) => { });

module.exports = router;